# Animation Frame

Engine-independent deterministic frame kernel for Scene V3.

This library owns frame/time derivation, semantic seed derivation, deterministic random streams, easing, keyframe evaluation, frame intervals and stable event schedules. It must not depend on browser clocks, wall-clock time, mutable global randomness or concrete animation engines.
