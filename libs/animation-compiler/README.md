# Animation Compiler

Engine-independent Scene V3 resolution and canonicalization pipeline.

This library converts validated authoring intent into an exact, portable `ResolvedSceneV3` identity. It owns source/evidence/runtime/asset resolution seams, canonical form v1, SHA-256 semantic hashing and deterministic compiler diagnostics. It does not render pixels or own wall-clock time.
