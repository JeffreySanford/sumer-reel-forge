# Animation Runtime

Engine-independent runtime adapter boundary for Scene V3.

This library owns runtime registration, capability preflight, adapter lifecycle, deterministic fake evaluation, failure injection and runtime evidence. It contains no concrete animation-engine dependency.
